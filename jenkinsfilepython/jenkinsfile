pipeline {
    agent any
    stages{
        stage('Fetch code'){
            steps{
                git credentialsId: 'BitBucket-Id', branch: '$BRANCH', url: '$URL_BITBUCKET'
            }
        }
        
        stage('Build'){
            steps{
                sh './python-build.sh'
            }
        }
    }
}