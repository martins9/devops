pipeline {
    agent any
    stages{
        stage('Fetch code'){
            steps{
                git credentialsId: 'BitBucket-Id', branch: '$BRANCH', url: '$URL_BITBUCKET'
            }
        }
        
        stage('Build'){
            steps{
                sh 'chmod +x python-build.sh'
                sh './python-build.sh'
            }
        }
    }
}