pipeline {
    agent any
    stages{
        stage('Fetch code'){
            steps{
                git credentialsId: 'BitBucket-Id', branch: '${params.BRANCH}', url: '${params.URL_BITBUCKET}'
            }
        }
        
        stage('Build'){
            steps{
                sh 'pyinstaller --onefile relatorio.py --name=relatorioLeite'
            }
        }
    }
}